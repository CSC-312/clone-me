pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: uvx ruff check .
      glob: "*.py"

    - name: format
      run: uvx ruff format .
      glob: "*.py"

    - name: check-types
      run: uvx ty check .
      glob: "*.py"
